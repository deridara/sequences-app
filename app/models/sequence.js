export default class Sequence {
    constructor (id, title, intervals, duration) {
        this.id = id;
        this.title = title;
        this.intervals = intervals;
        this.duration = duration;
    }
}