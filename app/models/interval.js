export default class Interval {
  constructor(id, title, duration, stopAfterFinish) {
    this.id = id;
    this.title = title;
    this.duration = duration;
    this.stopAfterFinish = stopAfterFinish;
  }
}
